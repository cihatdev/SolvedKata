const boolToWord = (bool: boolean): string => {
  let result: String = "";
  if (bool === false) {
    return "No";
  } else {
    ("Yes");
  }
};

let out: String = boolToWord(false);
console.log(out);
